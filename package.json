{
  "type": "module",
  "scripts": {
    "test": "node --test worker/src/index.test.mjs",
    "test:worker": "node --test worker/src/index.test.mjs",
    "worker:dev": "wrangler dev --test-scheduled",
    "worker:deploy": "wrangler deploy",
    "worker:tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
